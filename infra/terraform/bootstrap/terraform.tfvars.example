# Bootstrap values for the remote Terraform state bucket.
# Example values only; replace them before running bootstrap apply.

project_id = "your-gcp-project-id"
region = "us-east1"
state_bucket_name = "your-unique-haunted-halls-state-bucket"
